# n8n 文件夹说明

这里以后存放 n8n 工作流导出文件。

当前阶段还不放复杂 JSON。你先在 n8n 网页里手动创建工作流：

1. 登录 n8n。
2. 点左侧 `Workflows`。
3. 点 `Create Workflow`。
4. 工作流名称填：`每日 AI 抖音短视频草稿`。
5. 先保存。

下一步我们会按 `docs/n8n-workflow-design.md` 一步一步添加节点。

